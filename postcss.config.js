export default {
  plugins: {
    autoprefixer: {},
    // Optional: Only use cssnano if you want more aggressive 
    // optimization than Vite's default minifier.
    // cssnano: { preset: 'default' },
  },
};